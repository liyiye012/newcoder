class Frequency {
public:
    int getFrequency(vector<string> article, int n, string word) {
        // write code here
        int count=0;
        for(int i=0;i<article.size();i++)
            {
            if(word==article[i]){
                count++;
            }
        }
        return count;
    }
};
